# LuxCodercomp UI 优化计划（参考 workbuddy 设计）

> **日期**: 2026-08-05  
> **来源**: 参考 `workbuddy-ui-mockup.html` v3.6 设计文件  
> **原则**: 保持当前 project 风格；只保留 Agent 模式（Chat 废弃）；不改核心执行逻辑  
> **聚焦**: **pwork 模块是本次核心改造目标**；cowork 模块仅建空白功能页占位，内容待后续开发

---

## 总览

```
┌──────────────────────────────────────────────────────────────────┐
│                        优化范围总览                               │
├─────────────┬──────────────────┬─────────────────────────────────┤
│  A. 侧边栏   │ B. 右侧面板       │ C. Kanban 看板                  │
│  (核心改动)  │ (布局优化)        │ (设计借鉴)                      │
├─────────────┼──────────────────┼─────────────────────────────────┤
│ · 模式重命名 │ · Tab 栏提到顶部  │ · 列内按类型分组                │
│ · Chat 废弃  │ · 折叠/展开动画  │ · 优先级色条 + 类型徽章         │
│ · 常用插件   │ · 演示窗口 Tab   │ · 已完成半透明                  │
│ · Skills/MCP │ · 折叠图标条     │ · 同步按钮 (MCP 预留)           │
├─────────────┴──────────────────┴─────────────────────────────────┤
│ ⚠️ cowork 模块：建空白功能页占位，内容待后续开发                   │
│ ✅ pwork 模块：本次全部改动聚焦于此                                │
└──────────────────────────────────────────────────────────────────┘
```

---

## A. 侧边栏重构（LeftSidebar）

### A1. ModeSwitcher 重命名 ✅ 已完成（方向修正 2026-08-05）

| 改动前 | 改动后 | 内部值 |
|--------|--------|--------|
| `Home` (🏠) | `pwork` (🏠) | **`agent`**（编程模式） |
| `Code` (💻) | `cowork` (💻) | 占位（待开发） |

**⚠️ 方向修正记录**：
- 最初实现把 `pwork` 映射为 `chat`（对话模式）、`cowork` 映射为 `agent` —— **与用户需求相反**
- 用户澄清：**pwork 必须是 Agent 模式**，cowork 是占位；pwork 要补全原先 chat/Home 的菜单
- 正确方向：`pwork → agent`（编程工作区 + 融合 chat 菜单），`cowork → 占位页`
- **pwork**: Agent 模式主工作区，承载看板、会话、常用插件 + **融合 chat 的对话列表/置顶/Excalidraw/知识库菜单**——**本次核心改造目标**
- **cowork**: 点击后进入空白功能页（"协作功能开发中"），内部仍走 Agent 模式，侧边栏预留协作菜单入口
- **文件**: `ModeSwitcher.tsx` — ✅ 已完成

### A2. 废弃 Chat 模式，统一为 Agent 模式（方向修正后重述）

- **pwork = Agent 模式**（ModeSwitcher 内部值 `'agent'`），同时**补全原先 chat/Home 的菜单**
- 将 `LeftSidebar.tsx` 中 chat 专属区块（置顶对话、对话列表、Excalidraw、知识库）从 `mode === 'chat'` 条件中释放，改为在 **agent 模式（pwork）**下同样可见
- Chat 对话列表（`conversationsAtom`）融合进 Agent 会话列表的同一分组
- **文件**: `LeftSidebar.tsx`
- **风险**: 🟡 中，~400 行

### A3. 新增"常用插件"可展开菜单

```
🧩 常用插件  [▶]
  ⭐ 技能     [14]
  ⚙ MCP      [5]
```

- Skill/MCP 计数从 `workspaceCapabilitiesAtom` / workspace MCP 配置获取
- 点击子项 → `setSettingsTab('agent-skills')` / `setSettingsTab('agent-mcp')`
- **仅 pwork 侧边栏显示**
- **影响**: LeftSidebar.tsx 新增 ~60 行

### A4. 会话列表融合

```
改动前 (Code模式)                    改动后 (pwork模式)
┌────────────────────┐              ┌────────────────────┐
│ 项目分组            │              │ 📋 系统任务          │
├────────────────────┤              │ 🧩 常用插件          │
│ Agent 会话列表      │              │ 📚 知识库            │
│   · session 1     │              ├────────────────────┤
│   · session 2     │              │ 💬 会话 (融合)       │
└────────────────────┘              │   今天               │
                                    │     · agent session │
                                    │     · chat conv     │
                                    │   昨天               │
                                    │     ...             │
                                    └────────────────────┘
```

- **仅 pwork 侧边栏**，cowork 侧边栏为独立的空白占位

### A5. cowork 侧边栏 + 主区（占位模块，按设计蓝图搭架子）

```
┌── 侧边栏 ──────────────────┐  ┌────── 主区 ───────────────────────────────┐
│                            │  │                                           │
│  🤖 ABOT 对话    [开发中]   │  │  ┌─ Coworker 模块 ───────────────────────┐ │
│  💬 Slack集成    [开发中]   │  │  │                                       │ │
│  ✅ 审批流       [开发中]   │  │  │  🤖 ABOT 对话             [即将上线]  │ │
│  ❓ 问答         [开发中]   │  │  │  云端 Bot 主交互入口                     │ │
│  📚 知识库       [开发中]   │  │  │                                       │ │
│  💻 终端聚合     [开发中]   │  │  │  💬 Slack 集成            [即将上线]  │ │
│                            │  │  │  外部协作消息流接入                       │ │
│  ─────────────────────     │  │  │                                       │ │
│  💬 Bot 会话               │  │  │  ✅ 审批流                [即将上线]  │ │
│   (暂无)                   │  │  │  审批材料自动汇总与预审                   │ │
│                            │  │  │                                       │ │
│                            │  │  │  🖼️ 图像搜索              [即将上线]  │ │
│                            │  │  │  截图/设计稿/电路图匹配                  │ │
│                            │  │  │                                       │ │
│                            │  │  │  📖 SKILL Wiki            [即将上线]  │ │
│                            │  │  │  技能文档载体                            │ │
│                            │  │  │                                       │ │
│                            │  │  │  👥 成员管理              [即将上线]  │ │
│                            │  │  │  技能认证·权限                           │ │
│                            │  │  │                                       │ │
│                            │  │  │  💻 终端聚合              [即将上线]  │ │
│                            │  │  │  coderX | cursor | multica.ai          │ │
│                            │  │  └───────────────────────────────────────┘ │
└────────────────────────────┘  └───────────────────────────────────────────┘
```

- **侧边栏**: 6 个功能菜单项（ABOT/Slack/审批/问答/知识库/终端），全部灰色 + `[开发中]` 标签，不可点击
- **主区**: 7 张模块卡片（ABOT/Slack/审批/图像搜索/SKILL Wiki/成员管理/终端），全部显示 `[即将上线]` badge，hover 无交互
- 所有 UI 复用设计文件的模块卡片样式，降低后续填充时的迁移成本
- pwork ↔ cowork 切换正常，不阻断现有功能
- **新增文件**: `CoworkerPlaceholder.tsx`（~150 行）

---

## B. 右侧面板优化（RightSidePanel / SidePanel）

### B1. Tab 栏重构

```
改动前                              改动后
┌──────────────────────────┐       ┌─ 文件目录 ┬ 演示窗口 ┬ Diff ── [▶折叠]─┐
│ 文件浏览器               │       │ 🔍 搜索文件 ...                         │
│ [子tab: 会话/项目/改动]   │       │ [会话文件] [项目文件] [改动(3)]        │
└──────────────────────────┘       │ 📁 src/ · spi_driver.rs M · ...        │
                                   └───────────────────────────────────────┘
```

- Tab 栏从面板内部提到顶部
- **文件**: `SidePanel.tsx`、`RightSidePanel.tsx`
- **风险**: 🟡 中，~200 行

### B2. 折叠/展开 + 图标条

- 折叠后右侧面板收缩为 0（或极小宽），显示竖向图标条
- 点击图标恢复面板并切换到对应 Tab
- 复用现有 `agentSidePanelOpenAtom`

### B3. 演示窗口 Tab

- 复用现有 `previewFileMapAtom` + preview 能力，展示会话产出
- **低优先**，可后做

---

## C. Kanban 看板增强

### C1. 列内按类型分组折叠面板

```
改动前                              改动后
┌─────────────┐                    ┌──────────────┐
│ 待处理       │                    │ 待处理 (8)    │  ← 一级：状态列
│  ┌────────┐ │                    │ ┌ 🎯 活动 ──┐ │  ← 二级：类型分组
│  │ 任务A  │ │                    │ │ ▍P1 任务A │ │
│  │ 任务B  │ │                    │ │ ▍P2 任务B │ │
│  │ 任务C  │ │                    │ ├ 📋 需求 ──┤ │
│  │ 任务D  │ │                    │ │ ▍ 任务C  │ │
│  └────────┘ │                    │ ├ 🐛 缺陷 ──┤ │
└─────────────┘                    │ │ ▍P0 任务D │ │
                                   │ ├ ✅ 任务 ──┤ │
                                   │ │ ▍ 任务E  │ │
                                   │ └───────────┘ │
                                   └──────────────┘
```

- 类型分组: 🎯活动 / 📋需求 / 🐛缺陷 / ✅任务 / 📝Checklist
- Task 新增 `type` 字段，从现有 tag/label 映射
- **文件**: `WorkBoardView.tsx`
- **风险**: 🟡 中

### C2. 任务卡片视觉增强

```
改动前                              改动后
┌─────────────────────┐            ┌──────────────────────────────┐
│ 任务标题             │            │ ▍ P1  SPI驱动低温翻转排查    │ ← 优先级色条
│ 标签 & 负责人       │            │    VR-0832 · 待处理           │ ← 类型ID徽章
└─────────────────────┘            │    📁 芯片验证 · 👤 张工 · 08-05 │ ← 元数据行
                                   │    [自动] [执行] [→]           │ ← 操作按钮
                                   └──────────────────────────────┘
```

| 元素 | 实现 |
|------|------|
| **优先级色条** | 左侧 3px: P0=#ef4444, P1=#f59e0b, P2=#2563eb, P3=#9ca3af |
| **类型 ID 徽章** | 如 `VR-0832`，底色对应类型 |
| **已完成态** | opacity 0.65 + 隐藏操作按钮 |
| **自动执行** | `[自动]` toggle → task.autoExecute |
| **执行按钮** | `[执行]` → 触发 TaskRunner（已有） |

### C3. Kanban 同步按钮

```
┌─────────────────────────────────────────────────┐
│ 看板  ·  TB 同步 · 28 条                        │
│                    [🔄 ● 已同步]  12:30 更新     │
└─────────────────────────────────────────────────┘
```

| 状态 | 按钮表现 |
|------|---------|
| 空闲 | `[🔄 ● 已同步]`（绿点） |
| 同步中 | `[🔄 ○ 同步中…]`（icon 旋转 + 橙色脉冲） |
| 失败 | `[🔄 ✕ 同步失败]`（红点，hover 看原因） |

**三层实现路径**:

```
当前（Phase 4）          后续（MCP 集成阶段）
┌──────────────┐        ┌─────────────────────────┐
│ ① UI 按钮     │  ──▶  │ ② IPC 占位通道           │  ──▶  │ ③ MCP Server 接入
│   + 状态动画  │        │   sync-external-tasks   │        │    企业 bug 系统
│   + 时间戳    │        │   mock 返回             │        │    (Jira/禅道/飞书)
└──────────────┘        └─────────────────────────┘        └─────────────────────────┘
   本次做 ✅                  本次做 ✅                         后续做（MCP配置）
```

- **新增文件**: `WorkBoardHeader.tsx`（同步按钮组件，~120 行）
- **新增文件**: `main/lib/sync-external-tasks.ts`（占位 IPC 通道，~80 行，预留 MCP 调用接口）
- **风险**: 🟢 低

---

## D. 第二期（暂缓）

| 借鉴点 | 说明 | 依赖 |
|--------|------|------|
| **对话流中可折叠任务摘要卡片** | Agent 执行结果以卡片嵌入消息流 | 新组件 |
| **MCP 企业 bug 系统接入** | C3 同步按钮的后端实现 | MCP server 配置 |
| **cowork 模块功能填充** | Bot 对话、审批流、终端等 | 需求确认后迭代 |
| **输入区模型选择器内联** | 从 AgentHeader 移到输入区 | 与现有 Header 冲突 |

---

## 执行顺序

```
Phase 1 ✅          Phase 2 (2-3h)          Phase 3 (2h)
┌─────────────┐    ┌──────────────────┐    ┌─────────────────┐
│ A1 pwork/    │    │ A2 Chat模式废弃   │    │ B1 右侧面板Tab  │
│ cowork 改名  │ ─▶ │ A3 常用插件菜单   │ ─▶ │ B2 折叠/图标条  │
│              │    │ A4 会话列表融合   │    │                 │
└─────────────┘    │ A5 cowork 占位    │    └─────────────────┘
                   └──────────────────┘
                                      ↓
                              Phase 4 (3-4h)
                              ┌──────────────────┐
                              │ C1 Kanban类型分组 │
                              │ C2 任务卡片增强   │
                              │ C3 同步按钮       │
                              └──────────────────┘
```

每阶段独立可验证。

---

## 影响文件清单

| 文件 | Phase | 改动 | 行数 |
|------|-------|------|------|
| `ModeSwitcher.tsx` | 1 ✅ | 标签改名 | ~5 |
| `LeftSidebar.tsx` | 2 | Chat废弃 + 插件菜单 + 会话融合 + cowork占位 | ~500 |
| `CoworkerPlaceholder.tsx` | 2 | cowork 模块卡片 + 侧边栏占位（新增） | ~150 |
| `SidePanel.tsx` | 3 | Tab重构 | ~200 |
| `RightSidePanel.tsx` | 3 | 折叠/图标条 | ~150 |
| `WorkBoardView.tsx` | 4 | 列内类型分组 + 同步按钮嵌入 | ~250 |
| `TaskCard` 组件 | 4 | 视觉增强 | ~100 |
| `WorkBoardHeader.tsx` | 4 | 同步按钮 + 状态动画（新增） | ~120 |
| `sync-external-tasks.ts` | 4 | 占位 IPC + MCP 预留（新增） | ~80 |

---

## 不改动的部分

- ✅ Agent 核心执行逻辑（`agent-orchestrator.ts`）
- ✅ 会话管理（`agent-session-manager.ts`）
- ✅ Kanban/Task 调度（`task-runner.ts`）
- ✅ 主题系统（`theme.logic.ts`、`theme.seed.ts`）
- ✅ Settings（`SettingsPanel.tsx`）—— 插件子项仅跳转
- ✅ IPC 通信层
