# LuxCoder v0.1.98 更新

相对 `v0.1.97`：回退有问题的 macOS 更新兜底方案，恢复原先自动更新流程。

## 变更

- **回退 macOS「安装失败再下 DMG」方案** — 撤销 `v0.1.97` 引入的静默下载安装包 /「打开安装包」兜底逻辑（#22 回退 #20）。
- **恢复原自动更新主路径** — 仍为：检查更新 → 自动下载 →「立即重启」应用内安装。

## 说明

- Windows 更新路径未变。
- 当前 Release 若仍未配置 Apple Developer ID 签名/公证，macOS 上应用内「立即重启」可能继续失败；可从本页下载 DMG 手动覆盖安装（见下方打开说明）。

## macOS 打开说明（重要）

当前 Release **未配置 Apple Developer ID 签名/公证**。从浏览器下载后，macOS Gatekeeper 可能误报：

> “LuxCoder” is damaged and can’t be opened.

**这不是安装包损坏。** 安装到 Applications 后执行：

```bash
xattr -cr /Applications/LuxCoder.app
open /Applications/LuxCoder.app
```

若还在 Downloads 里的 `.app` / `.dmg`，先对对应路径跑同样的 `xattr -cr`。

## 下载

- **macOS Apple Silicon** — `LuxCoder-0.1.98-arm64.dmg` / `LuxCoder-0.1.98-arm64-mac.zip`
- **Windows** — `LuxCoder-Setup-0.1.98.exe`
